#include <stdio.h>

int main() {
    int i,x,y,w,z;
    for ( i = 0; i <= 1000 ; ++i)
    {
        x = i;
        y = x + 1;
        w = x^2 + y^2;
        z = w^2;
        if ()
    }
    return 0;
}
